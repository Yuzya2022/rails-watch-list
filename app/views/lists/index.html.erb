<h1>My lists</h1>
<ul>
<% @lists.each do |list| %>
<li><%= link_to list.name, list_path(list) %></li>
<% end %>
</ul>
<div class="btn btn-success d-flex justify-content-center text-center mb-5">
<%= link_to "Create new list", new_list_path %>
</div>
